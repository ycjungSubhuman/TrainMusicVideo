/* stores Action objects */
(function (scope) {
	this.ActionManager = {
		init: function () {
			this.list_action = new Array();
		},
		addAction: function (action) {

		}
	}
	ActorManater.init();
}) (this);